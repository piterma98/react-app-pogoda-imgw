import React from 'react';

const Home = (props: any) => {
    console.log(props);

    return (
        <div>
            <h2>React app for fetching imgw data from api</h2>
        </div>
    );
};

export default Home;