import React from 'react';

const Home = () => {
    return (
        <div>
            <h2 style={{textAlign: "center"}}>React app for fetching imgw data from api</h2>
        </div>
    );
};

export default Home;